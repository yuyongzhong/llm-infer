## ========== 基本配置 ==========
model_name="deepseek"                     # 模型名称，用于日志命名、推理调用等
HOME_PATH="/home/yuyongzhong"            # 代码根目录，用于构造路径
LOG_INFO="yyz_test_20250709_1736"        # 当前测试任务标识，会用于输出日志文件夹命名
BASE_INFO="112机器 模型：Qwen-1.5B"       # 基础信息（用于飞书通知中展示）

# 控制主运行流程：仅跑精度评估、仅跑性能评估、二者先后顺序
# 可选值：accuracy | benchmark | acc-then-bench | bench-then-acc
RUN_MODE=bench-then-acc


## ========== 精度评估配置 ==========
api_url="http://127.0.0.1:8000/v1"        # 调用大模型服务的 API 地址（用于精度测试）
temperature=0.6                           # 控制生成文本的随机性（越大越随机）
top_p=0.95                                # nucleus sampling 中的概率阈值
use_cache=""                              # 是否使用缓存（保留空字符串时视为不传）
max_tokens=3000                           # 每轮生成最大 token 数
datasets="ceval"                          # 评估使用的数据集，多个用逗号分隔
data_mode="all"                           # 数据集加载模式：subset（只测试一部分）或 all（全量）
answer_num=2                              # 每个问题生成答案的次数
eval_batch_size=15                        # 精度测试请求时的批处理大小


## ========== 飞书通知相关 ==========
webhook_url="https://oapi.dingtalk.com/robot/send?access_token=9ad9373a15c82ad31bca9da0d92f8602432b79c3ae5975bc6160cf9ab5d82b49"  # 飞书/钉钉机器人 webhook
CHECK_INTERVAL=60                           # 精度测试运行中监控间隔（秒）


## ========== 性能评估配置 ==========
BASE_URL="http://127.0.0.1:8000"           # 推理服务基地址（无 /v1 后缀）
TOKENIZER_PATH="/home/yuyongzhong/models/DeepSeek-R1-32B"  # tokenizer 路径（传给性能测试脚本）

# 并发请求设置：支持多组 batch size
BATCH_SIZES="16 32"                        # 用空格分隔的 batch size 列表

# 输入输出长度组合：用分号隔开每组，用空格隔开长度对
# 格式："输入长度 输出长度;输入长度 输出长度;..."
PROMPT_PAIRS="128 128;128 64;"            

NUM_PROMPTS=10                             # 每组配置运行请求的样本数

#####完整的配置
# BATCH_SIZES="1 4 8 16 32"
# PROMPT_PAIRS="128 128;256 256;1024 1024;2048 200;2048 1024"
# NUM_PROMPTS=200